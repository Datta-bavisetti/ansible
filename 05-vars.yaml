- name: external variables
  hosts: ansible
  vars_files:
    empinformation.yaml
  tasks:
  - name: printing the employee information
    ansible.builtin.debug:
      msg: My name is {{ NAME }}, working as {{ ROLE }}. I joined wipro on {{ DOJ }}.
      

