- name: shell and command usage in ansible
  hosts: local
  connection: local

  tasks:
  - name: command test
    ansible.builtin.command: cat /etc/passwd | grep root
    register: user_output

  - name: print output
    ansible.builtin.debug:
      msg: "{{ user_output }}"