- name: condtions in ansible
  hosts: local
  connection: local
  vars:
    age: 24

  tasks:
  - name: check the age is greater than 18
    ansible.builtin.debug:
     msg: "Age is greater than 18"
    when: age > 18
